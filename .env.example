# Copy to .env and fill in. Never commit .env. Names only here, no values.
# Model providers (only the ones you use)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_VERSION=
OPENROUTER_API_KEY=
# Free Law Project (agent 002); optional but strongly recommended
COURTLISTENER_TOKEN=
# Harness runtime selection (CLI flags override these)
LA_PROVIDER=
LA_MODEL=
LA_ROUTE=
LA_EFFORT=
LA_REVIEWER=
# SEC fair-access requires a real contact in the user agent
EDGAR_USER_AGENT="Hassaan Mallick legal-agents research (contact@mallick.tech)"
