"""Importers for public labelled datasets. They produce corpus documents plus
DRAFT golden lines. Public labels use someone else's definition of correct, so
every imported line stays `labelled_by: draft…` until a person checks it."""
